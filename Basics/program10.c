// C program to find the area of a circle.
// Multiplication with decimal numbers.

#include<stdio.h>
#include<conio.h>

void main()
{
    int r=15; // r = radius of circle
    float area=3.142*r*r; // area = pi*r*r
    clrscr();
    printf("area=%f",area);
    getch();
}
// arrays - collection of similar elements
// Compile time initialisation
// along with declaration


#include <stdio.h>

int main()
{
    // int a; // one data item
    int a[4]={10,20,30,40}; // group of data item - collection/group of 5 integers
    printf("\na[0] = %d",a[0]);
    printf("\na[1] = %d",a[1]);
    printf("\na[2] = %d",a[2]);
    printf("\na[3] = %d",a[3]);
    return 0;
}